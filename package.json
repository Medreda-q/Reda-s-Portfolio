{
  "name": "reda-portfolio",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "hugo -s cv-project -d ../public/cv --gc --minify || npx hugo-extended -s cv-project -d ../public/cv --gc --minify && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^8.0.12"
  }
}
